name: Hello world
on: 
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
          - name: buildsteps
            run: echo "hi build1"
  tests:
    runs-on: ubuntu-latest
    needs: build
    steps:
        - name: teststeps
          run: echo " this is test1"
  deploy:
    runs-on: ubuntu-latest
    needs: tests
    steps:
      - name: depo
        run: echo " his is depopy1"

  
